spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: image, mysql
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@202.120.40.8:30743/eureka/
