spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: activity, mysql, mongo, uniconfig
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@bamdb_registry:8761/eureka/
