spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: activity, mysql
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/
