spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: auth, redis, uniconfig, rabbitmq, mongo
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@registry:8761/eureka/
