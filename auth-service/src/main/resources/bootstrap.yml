spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: auth, redis, uniconfig
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@bamdb_registry:8761/eureka/
