spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: auth, redis, uniconfig, rabbitmq
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@registry:8761/eureka/
