spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      fail-fast: true
      name: gateway, ribbon
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/
