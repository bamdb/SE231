<<<<<<< HEAD:code/backend/gateway/src/main/resources/bootstrap.yml
spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: gateway, ribbon, rabbitmq
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@registry:8761/eureka/
=======
spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: gateway, ribbon, rabbitmq
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@registry:8761/eureka/
>>>>>>> master:code/backend/gateway/src/main/resources/bootstrap.yml
