spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: item, mysql, mongo, uniconfig
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/

