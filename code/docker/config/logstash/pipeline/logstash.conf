input {
    rabbitmq {
        host => "localhost"
        port => 15672
        heartbeat => 30
        durable => false
        exchange => "bamdb"
        password => "guest"
        user => "guest"
    }
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "bamdb"
	}
}