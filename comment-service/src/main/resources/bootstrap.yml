spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: comment, mongo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
