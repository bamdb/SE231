spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: comment, mongo
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/
