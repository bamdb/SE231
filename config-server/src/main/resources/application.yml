spring:
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config
  application:
    name: config
server:
  port: 8888
eureka:
  client:
    register-with-eureka: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
