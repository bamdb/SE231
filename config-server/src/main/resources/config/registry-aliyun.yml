spring:
  application:
    name: registry
  security:
    user:
      password: bamdb
      name: bamdb
eureka:
  instance:
    prefer-ip-address: true
  client:
    registerWithEureka: false
    fetchRegistry: false
    service-url:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/,  http://bamdb:bamdb@202.120.40.8:30743/eureka/
  server:
    eviction-interval-timer-in-ms: 30000
server:
  port: 8761