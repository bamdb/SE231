spring:
  cloud:
    config:
      discovery:
        enabled: false
      enabled: false
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@localhost:8761/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'