spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: gateway, ribbon
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@bamdb_registry:8761/eureka/
