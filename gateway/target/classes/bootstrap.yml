spring:
  cloud:
    config:
      dscovery:
        service-id: config
        enabled: true
      fail-fast: true
      name: gateway
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
