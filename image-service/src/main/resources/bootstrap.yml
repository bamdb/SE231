spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: image, mongo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
