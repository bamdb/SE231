spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: rating, mysql, uniconfig
  main:
    allow-bean-definition-overriding: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://bamdb:bamdb@bamdb_registry:8761/eureka/
