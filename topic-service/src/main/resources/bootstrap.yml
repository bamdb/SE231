spring:
  cloud:
    config:
      discovery:
        service-id: config
        enabled: true
      name: topic, mysql, mongo
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/